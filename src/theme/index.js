/**
 * Central Theme Configuration
 * Import all theme configurations in one place
 */

import colors from "./colors";
import typography from "./typography";
import { spacing, layout, breakpoints } from "./layout";

export const theme = {
  colors,
  typography,
  spacing,
  layout,
  breakpoints,
};

export { colors, typography, spacing, layout, breakpoints };
export default theme;
